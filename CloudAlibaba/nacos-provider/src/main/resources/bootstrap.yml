server:
  port: 8800
spring:
  #application:
  #  name: nacos-provider
  #cloud:
  #  nacos:
  #    config:
  #      server-addr: 120.79.4.13:8500
  #      file-extension: yaml
  #      prefix: nacos
  #    discovery:
  #      server-addr: 120.79.4.13:8500
  #  sentinel:
  #    transport:
  #      dashboard: sentinel.zhaosj.cn
  #      port: 8719
  # profiles:
  #  active: provider
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      # 连接池的配置信息
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://120.79.4.13:3306/nacos_config?useUnicode=true&characterEncoding=UTF-8
      username: root
      password: admin123
      # 初始化大小，最小，最大
      initial-size: 5
      min-idle: 5
      maxActive: 20
      # 配置获取连接等待超时的时间
      maxWait: 60000
      # 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒
      timeBetweenEvictionRunsMillis: 60000
      # 配置一个连接在池中最小生存的时间，单位是毫秒
      minEvictableIdleTimeMillis: 300000
      validationQuery: SELECT 1 FROM SYS_USER
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      # 打开PSCache，并且指定每个连接上PSCache的大小
      poolPreparedStatements: true
      maxPoolPreparedStatementPerConnectionSize: 20
      # 配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
      filters: stat,wall,log4j
      #filters: stat,wall
      # 通过connectProperties属性来打开mergeSql功能；慢SQL记录
      connectionProperties: druid.stat.mergeSql\=true;druid.stat.slowSqlMillis\=5000

  data:
    elasticsearch:
      cluster-name: docker-cluster
      cluster-nodes: 120.79.4.13:8300

mybatis:
  type-aliases-package: com.zsj.nacos.mapper
  mapper-locations: classpath:mapper/*.xml

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count = countSql
  offset-as-page-num: true

